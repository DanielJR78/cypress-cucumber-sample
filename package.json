{
  "name": "cypress-cucumber-example",
  "version": "1.0.0",
  "description": "Example of cypress with cucumber",
  "main": "index.js",
  "scripts": {
    "open": "node_modules/.bin/cypress open",
    "test": "node_modules/.bin/cypress run",    
    "report_mocha_merge": "npx mochawesome-merge  cypress/reports/mocha/*.json> cypress/reports/report.json; rd -r cypress/reports/mocha",
    "report_junit_merge": "junit-merge -d cypress/results/single -o cypress/results/merge/result-merge.xml"
  },  
  "keywords": ["cypress", "cucumber", "testing"],
  "author": "Daniel JR",
  "homepage": "https://github.com/DanielJR78/cypress-cucumber-sample#readme",
  "dependencies": {
    "cypress": "^5.5.0",
    "cypress-cucumber-preprocessor": "^2.5.0",
    "cypress-multi-reporters": "^2.0.0",
    "mocha-junit-reporter": "^2.0.0",
    "protractor": "^7.0.0",
    "cucumber-html-reporter": "^5.0.0",
    "junit-merge": "^2.0.0"
  },
  "cypress-cucumber-preprocessor": {
    "nonGlobalStepDefinitions": true
  },
  "devDependencies": {    
    "@testing-library/cypress": "^7.0.1",
    "mocha": "^8.0.0",
    "mochawesome": "^6.2.1",
    "mochawesome-merge": "^4.2.0",
    "mochawesome-report-generator": "^5.1.0"
  }
}
